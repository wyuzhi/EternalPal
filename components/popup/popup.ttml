<view tt:if="{{show}}" class="popup-mask" style="z-index: {{zindex}};" bindtap="onMaskTap" catchtouchmove="onTouchMove">
  <view class="popup-container {{position}}" catchtap="onContainerTap">
    <slot></slot>
  </view>
</view>