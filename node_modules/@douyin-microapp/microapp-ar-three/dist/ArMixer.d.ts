import type THREE from 'three';
import { ArProcessor } from './ArProcessor';
interface ArMixerParams {
    three: typeof THREE;
    processors: ArProcessor[];
    cameraTexture: THREE.Texture;
    modelTexture: THREE.Texture;
    mixedTextureWidth: number;
    mixedTextureHeight: number;
}
interface RenderParams {
    renderer: THREE.WebGLRenderer;
}
export declare class ArMixer {
    private mixedRenderTarget;
    private maskScene;
    private maskCamera;
    private three;
    mixedTexture: THREE.Texture;
    processors: ArProcessor[];
    constructor({ three, processors, cameraTexture, modelTexture, mixedTextureWidth, mixedTextureHeight, }: ArMixerParams);
    render({ renderer }: RenderParams): void;
}
export {};
