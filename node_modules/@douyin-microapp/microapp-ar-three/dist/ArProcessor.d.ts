export declare enum MaskType {
    remove = 0,
    reveal = 1
}
export interface ArProcessor {
    name: string;
    maskTextures?: THREE.DataTexture[];
    maskType?: MaskType;
    threshold?: number;
}
