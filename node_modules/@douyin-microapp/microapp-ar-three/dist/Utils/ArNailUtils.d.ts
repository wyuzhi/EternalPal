import type THREE from 'three';
interface GetNailModelGroupByColorParams {
    THREE: typeof THREE;
    renderer: THREE.WebGLRenderer;
    color: THREE.Color | string | number;
}
interface GetNailModelGroupByTextureParams {
    THREE: typeof THREE;
    renderer: THREE.WebGLRenderer;
    texture: THREE.Texture;
}
declare const ArNailUtils: {
    getNailModelGroupByColor: ({ THREE, renderer, color, }: GetNailModelGroupByColorParams) => THREE.Group;
    getNailModelGroupByTexture: ({ THREE, texture, }: GetNailModelGroupByTextureParams) => THREE.Group;
};
export { ArNailUtils };
